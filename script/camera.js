function openFileExplorer() {
    // Récupère l'élément input pour les fichiers
    var fileInput = document.getElementById("fileInput");
    // Déclenche le clic sur l'élément input pour ouvrir l'explorateur de fichiers
    fileInput.click();
}
